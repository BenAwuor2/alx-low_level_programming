3include "main.h"
/**
 * _isupper -evalue if letter is uppercase
 * @c: print int
 * Return: 1 if c is uppercase, 0 otherwise.
 */

itn _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}

else
return (0);
}
